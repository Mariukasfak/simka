# Palette's Journal

## 2025-05-15 - Form Accessibility Pattern
**Learning:** Using `aria-invalid`, `aria-describedby`, and `role="alert"` significantly improves form accessibility for screen readers by providing immediate feedback and context.
**Action:** Always link form inputs to their error messages with `aria-describedby` and ensure error messages have unique IDs and `role="alert"`.
