## 2024-05-15 - ARIA Labels for Icon-Only Buttons
**Learning:** Icon-only buttons (like `X` to close tooltips/modals) need explicit `aria-label` attributes localized to the application's language (Lithuanian) for screen reader support.
**Action:** Always verify icon-only buttons (`<X />`, `<TrashIcon />`, etc.) have `aria-label` attributes.
