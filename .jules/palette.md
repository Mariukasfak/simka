## 2024-05-22 - Accessibility Improvement in Order Form
**Learning:** Adding standard ARIA attributes (`aria-invalid`, `aria-describedby`, `aria-required`) and semantic grouping (`fieldset`/`legend`) significantly improves the form experience for screen reader users without affecting visual design.
**Action:** Apply these patterns to all new form components by default.
